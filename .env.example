# Duffel test/sandbox API key (starts with "duffel_test_").
# Get it from https://app.duffel.com/settings/access-tokens
# Copy this file to .env.local and fill in the real value — .env.local is
# git-ignored and never leaves this machine.
DUFFEL_API_KEY=
